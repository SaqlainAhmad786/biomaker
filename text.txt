// DOWNLOAD AS WORD
            //         const blob = window.htmlDocx.asBlob(content); // Access it via 'window.'

            //         window.onload = () => {
            //             document.getElementById('download-btn').addEventListener('click', () => {
            //                 try {
            //                     const section = document.getElementById('capture-section').outerHTML;

            //                     const content = `
            //     <!DOCTYPE html>
            //     <html>
            //       <head><meta charset="UTF-8"><title>Document</title></head>
            //       <body>${section}</body>
            //     </html>
            //   `;

            //                     // Use window.htmlDocx to ensure library reference
            //                     const blob = window.htmlDocx.asBlob(content);
            //                     const link = document.createElement('a');
            //                     link.href = URL.createObjectURL(blob);
            //                     link.download = 'section.docx';
            //                     link.click();
            //                 } catch (error) {
            //                     console.error('Error:', error);
            //                     alert('An error occurred while generating the Word document.');
            //                 }
            //             });
            //         };